def scriptFile = project.buildscript.sourceFile
def flutterRoot = scriptFile.parentFile.parentFile

android {
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
